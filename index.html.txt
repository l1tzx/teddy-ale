<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Teddy & Ale ❤️</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    :root{
      --rosa:#ff4d88;
      --rosa2:#ff7fb3;
      --oscuro:#0b0b10;
      --texto:rgba(255,255,255,0.92);
      --suave:rgba(255,255,255,0.65);
      --carta:rgba(255,255,255,0.08);
      --borde:rgba(255,255,255,0.15);
    }

    body{
      margin:0;
      font-family:Arial, sans-serif;
      background:
      radial-gradient(circle at 20% 10%, rgba(255,77,136,0.3), transparent 50%),
      radial-gradient(circle at 80% 90%, rgba(255,77,136,0.2), transparent 50%),
      var(--oscuro);
      color:var(--texto);
      text-align:center;
      padding:40px;
    }

    h1{
      font-size:60px;
      margin-bottom:5px;
    }

    .sub{
      color:var(--suave);
      margin-bottom:20px;
    }

    .corazon{
      font-size:80px;
      animation:latido 1.5s infinite;
    }

    @keyframes latido{
      0%,100%{transform:scale(1);}
      50%{transform:scale(1.2);}
    }

    .card{
      background:var(--carta);
      border:1px solid var(--borde);
      padding:20px;
      border-radius:15px;
      max-width:500px;
      margin:20px auto;
    }

    button{
      background:linear-gradient(45deg,var(--rosa),var(--rosa2));
      border:none;
      padding:15px;
      border-radius:10px;
      color:white;
      font-size:16px;
      cursor:pointer;
      margin-top:10px;
    }

    button:hover{
      opacity:0.8;
    }

    footer{
      margin-top:30px;
      color:var(--suave);
      font-size:14px;
    }

  </style>
</head>

<body>

<h1>Teddy & Ale</h1>

<div class="sub">
Una página solo para nosotros dos 💕
</div>

<div class="corazon">❤️</div>


<div class="card">

<h3>Nuestra historia</h3>

<p id="mensaje">
Teddy y Ale… no es perfecto, pero es real.  
Contigo me siento feliz, tranquilo y completo.
</p>

<button onclick="cambiarMensaje()">
Presiona aquí
</button>

</div>


<footer>
Hecho con amor 💌
</footer>


<script>

function cambiarMensaje(){

const mensajes = [

"Teddy ❤️ Ale — siempre juntos.",

"Eres mi persona favorita.",

"Pase lo que pase, siempre tú.",

"Contigo todo se siente mejor.",

"Solo tú y yo 🫶"

];

const random =
mensajes[Math.floor(Math.random()*mensajes.length)];

document.getElementById("mensaje").textContent=random;

}

</script>

</body>
</html>